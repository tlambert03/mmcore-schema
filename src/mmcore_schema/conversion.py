"""Utility functions for converting between config file formats."""

from pathlib import Path


def convert_file(input: str | Path, output: str | Path) -> str:
    """Convert a JSON file to a JSON schema."""
    return ""
